{
  "erd_version": 82,
  "tasks": [
    {
      "brief": "**Implementation constraints (FIRST):** JavaScript in the existing file; edit via anchored SEARCH/REPLACE blocks (D-59) \u2014 never retype unchanged regions. Change nothing outside the message-input keydown handler and its immediate surroundings. Do not alter the submit handler's empty-input guard (trims and no-ops on empty) and keep the `isComposing` IME guard. No new libraries, no new globals.\n\n**Behavioral specification:** the message-input keydown handler currently submits the form on plain Enter and lets Shift+Enter keep the textarea's default newline behavior. Invert it: plain Enter keeps the default newline (never sends); Shift+Enter keeps the default newline; Ctrl+Enter or Cmd+Enter submits the form through the same `form.requestSubmit()` path the current handler uses.\n\n**Acceptance condition:** the frozen tests `test_message_input_plain_enter_inserts_newline_without_sending`, `test_message_input_ctrl_enter_sends`, `test_message_input_cmd_enter_sends`, `test_message_input_ctrl_enter_empty_sends_nothing`, and `test_no_loaded_model_shows_placeholder_and_send_guides` (all in tests/test_ui.py) pass. The rename-input Enter-commit behaviors (AC-114, `thread-rename-input` / `current-thread-title-input`) are untouched.",
      "contracts": [],
      "depends_on": [],
      "file": "src/static/app.js",
      "id": "T1",
      "tests": []
    },
    {
      "brief": "**Implementation constraints (FIRST):** HTML in the existing file; anchored edit of the message-input element's placeholder attribute only; no other markup changes; no script changes.\n\n**Behavioral specification:** update the message-input element's placeholder text to \"Type a message... (Ctrl+Enter to send, Enter for newline)\". Nothing else changes.\n\n**Acceptance condition:** the frozen test `test_message_input_placeholder_states_shortcuts` (tests/test_ui.py) passes.",
      "contracts": [],
      "depends_on": [
        "T1"
      ],
      "file": "src/static/index.html",
      "id": "T2",
      "tests": [
        "tests/test_ui.py::test_message_input_cmd_enter_sends[chromium]",
        "tests/test_ui.py::test_message_input_ctrl_enter_empty_sends_nothing[chromium]",
        "tests/test_ui.py::test_message_input_ctrl_enter_sends[chromium]",
        "tests/test_ui.py::test_message_input_placeholder_states_shortcuts[chromium]",
        "tests/test_ui.py::test_message_input_plain_enter_inserts_newline_without_sending[chromium]",
        "tests/test_ui.py::test_no_loaded_model_shows_placeholder_and_send_guides[chromium]"
      ]
    }
  ],
  "version": 1
}
