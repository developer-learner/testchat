# T9 completion review — 2026-09-08

The Vortex cutover now provides ready-only Vortex/Local optgroups, the Vortex
dashboard link, a source indicator, and local fallback. Local load/unload/Eject
controls and their handlers are removed. Backend routing remains unchanged.

## Build and repair provenance

- v125 corrected the stale picker contract and specified promise/readiness
  integration. The existing six T9 tests were not changed.
- The EM plan validated. Two 4,096-token coder edit replies were truncated.
  A bounded 8,192-token attempt produced the grouped picker (`9a73881`), but
  failed the carried router-picker test because it hid router options when the
  fixture omitted router-status metadata.
- At that failure checkpoint, D-132 applied: Codex directly repaired the small,
  identified defects in catalog.js and app.js. The picker retains router options,
  preserves the frozen empty-selection wording, compares model IDs without CSS
  interpolation, and marks unreachable router options unready. The source helper
  reads the selected group instead of an unpopulated cache. Model loading,
  hydration, new-chat and sidebar selection refresh the status; keyboard sends
  reject an unavailable selection.
- Browser review found stale load/unload modal listeners in chrome.js. Removing
  those listeners and Escape branches completes the lifecycle UI removal and
  preserves settings/delete modal dismissal.
- These repairs were made by Codex under D-132, not generated by the 27B coder.
  No pipeline counters or completion markers were manually marked successful.

## Verification before merge

- Red baseline: 3/6 T9 tests failed in the sandbox before the rebuild.
- Final frozen suite: **237/237 sandbox**, **237/237 macOS**, including all six
  T9 tests. Only dependency deprecation warnings remain.
- JavaScript syntax checks pass for catalog.js, app.js and chrome.js;
  git diff whitespace check passes.
- Isolated browser review: online New Chat selects Vortex and shows via Vortex;
  offline New Chat selects Local and shows via local; Send is enabled for that
  ready fallback; disabled Manage opens no new tab; Escape closes settings;
  both scenarios emit zero page errors.
- Existing frozen routing tests cover the router/local chat paths. Browser
  checks used simulated model responses, not a live inference session.
- Frozen tests are unchanged since v124. The removed-dialog cleanup was a
  review finding beyond those six tests; the full suite was rerun afterward.

## Closeout

Acceptance-only v126 protected the verified files from further coder edits.
The orchestrator ran the full frozen suite and recorded nine durable task
completions at **6cb5b67**, `[success] spec v126`. No coder was invoked during
closeout. This does not claim that the local coder completed every repair.
Main's unrelated TESTCHAT_COVERAGE_HANDOFF.md is outside this work.
